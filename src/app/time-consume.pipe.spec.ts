import { TimeConsumePipe } from './time-consume.pipe';

describe('TimeConsumePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeConsumePipe();
    expect(pipe).toBeTruthy();
  });
});
